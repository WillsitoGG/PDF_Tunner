name: PDF_Tunner OCRmyPDF Candidate (Retired)

# Retired after primary Run #77 accepted Python + OCRmyPDF.
# Kept manual-only temporarily so the retirement can be committed atomically
# with README.md and AGENTS.md; remove this file during final CI cleanup.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired candidate gate
        run: echo "OCRmyPDF candidate gate retired after primary Run #77; use pdf-tunner-windows-portable.yml."
